mutation UpsertQuestionVote($questionId:ID!, $upDown:Boolean!) {
    upsertQuestionVote(questionId:$questionId, upDown:$upDown) {
        question {
            voteScore
            userDidVote
        }
    }
}