query GetQuestions($cID:ID!) {
    getQuestions(categoryId: $cID) {
        id
        title
        description
        voteScore
        clickScore
        userDidVote
        userDidClick
        categories {
            name
        }
        user {
            name
        }
    }
}