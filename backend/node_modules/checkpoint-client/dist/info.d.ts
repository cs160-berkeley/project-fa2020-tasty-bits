import { Check } from './index';
export interface Information {
    signature: string;
    cachePath: string;
    cacheItems: Check.Schema[];
}
export declare function getInfo(): Promise<Information>;
